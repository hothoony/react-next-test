import styles from '@/styles/common.module.css';

const teamView = () => {
    return (
        <div className={styles.centerMiddle}>
            <h1>teamView</h1>
        </div>
    );
}

export default teamView;
